# Asset Processing Tools

Scripts for processing source artwork from `art/source/` into runtime assets in `public/game-assets/`.

To be expanded in milestone `v0.0.4` using non-destructive image extraction.
